{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "framework:null is load-bearing. Left to itself Vercel sees the Python dep and builds the repo as a Python *app*, where one entrypoint answers every route and the atlas vanishes behind /api/ask. Null keeps it a static site whose api/*.py files are individual functions.",
  "framework": null,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" }
      ]
    },
    {
      "source": "/data/(.*).json",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }
      ]
    }
  ]
}
